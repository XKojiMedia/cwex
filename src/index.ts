import { buildProject } from './commands/build';

// binary file
console.log('We compile');
// inquirer

// scaffold project

// build project
// Compile assets:
/*
 out/
  chrome-extension/
    includes**
    manifest.json
  mozilla-extension/
    includes**
    manifest.json
*/
buildProject();
